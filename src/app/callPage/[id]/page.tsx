import React from "react";

type Props = {
  params: {
    id: string;
  };
};

const page = ({ params: { id } }: Props) => {
  return <main className="max-w-7xl mx-auto">d</main>;
};

export default page;
